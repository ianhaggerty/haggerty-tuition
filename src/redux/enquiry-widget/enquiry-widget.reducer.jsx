const INITIAL_STATE = {
  currPage: 0
};

const enquiryWidgetReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default enquiryWidgetReducer;
